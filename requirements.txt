# Керування браузером — через CDP поверх aiohttp, тому ні playwright, ні selenium тут
# немає і бути не повинно: вони тягнуть власні рантайми, а вимога проєкту — один .exe.
aiohttp>=3.9,<4.0
yarl
truststore

# трей та іконка
Pillow
pystray

# сучасні віджети вікна: заокруглення, тумблери, власний скролбар.
# Тримається на самому tkinter, окремого рантайму не тягне — вимога «один .exe»
# не порушується. ⚠️ Теми лежать у JSON поруч із пакетом, тому build.spec мусить
# їх зібрати: без цього .exe падає на старті, хоч локально все працює.
customtkinter

# сповіщення трею на Windows
pywin32; sys_platform == "win32"

# підпис манифесту оновлень (Ed25519)
cryptography
